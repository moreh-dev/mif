apiVersion: odin.moreh.io/v1alpha1
kind: InferenceServiceTemplate
metadata:
  name: vllm-decode
  namespace: {{ include "common.names.namespace" . }}
  labels:
    {{- include "mif.runtimeBase.labels" . | nindent 4 }}
    mif.moreh.io/role: decode
spec:
  framework: vllm
  template:
    metadata:
      labels:
        mif.moreh.io/role: decode
    spec:
      imagePullSecrets:
        - name: moreh-registry
      initContainers:
        - name: proxy
          image: 255250787067.dkr.ecr.ap-northeast-2.amazonaws.com/heimdall-proxy:v0.7.0-rc.4
          restartPolicy: Always
          command:
            - /bin/bash
            - -c
          args:
            - |
              set -ex

              exec /app/proxy \
                --port 8000 \
                --decoder-ip $(POD_IP) \
                --decoder-port 8200 \
                $(ISVC_EXTRA_ARGS)
          env:
            - name: ISVC_EXTRA_ARGS
              value: >-
                --pd-coordinator vllm/nixl
                --log-format json
                --log-level warn
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          readinessProbe:
            httpGet:
              path: /health-proxy
              port: 8000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
      containers:
        - name: main
          image: 255250787067.dkr.ecr.ap-northeast-2.amazonaws.com/quickstart/moreh-vllm:v0.15.1.1
          securityContext:
            capabilities:
              add:
                - IPC_LOCK
          command:
            - /bin/bash
            - -c
          args:
            - |
              set -ex

              if [ -e "$ISVC_PRESET_PATH" ]; then
                eval "$(
              {{- .Files.Get "files/convert_preset.sh" | nindent 14 -}})"
              fi

              # Clear /dev/shm on start to prevent running out of space when crashes occur
              # https://github.com/llm-d/llm-d/issues/352
              find /dev/shm -type f -delete

              cleanup() {
                echo "Received SIGTERM, killing child processes..."
                pkill -P $$
                wait
              }
              trap cleanup SIGTERM SIGINT

              if [ -n "$ISVC_PRE_PROCESS_SCRIPT" ]; then
                echo "Running pre-process script..."
                eval "$ISVC_PRE_PROCESS_SCRIPT"
              fi

              _kv_events_args=()
              if [ "${ISVC_USE_KV_EVENTS:-false}" = "true" ]; then
                _inference_pool_name="{{ "{{" }} if .Spec.InferencePoolRefs {{ "}}" }}{{ "{{" }} (index .Spec.InferencePoolRefs 0).Name {{ "}}" }}{{ "{{" }} end {{ "}}" }}"
                if [ -z "$_inference_pool_name" ]; then
                  echo "Set inferencePoolRefs in InferenceService"
                  exit 1
                fi
                if [ -z "{{ "{{" }} deref .Spec "Model" "Name" {{ "}}" }}" ]; then
                  echo "Set spec.model.name in InferenceService"
                  exit 1
                fi
                _kv_events_args=(--kv-events-config "{ \
                  \"enable_kv_cache_events\": true, \
                  \"publisher\": \"zmq\", \
                  \"endpoint\": \"tcp://${_inference_pool_name}:5557\", \
                  \"topic\": \"kv@${POD_IP}:8000@{{ "{{" }} deref .Spec "Model" "Name" {{ "}}" }}\" \
                }")
              fi

              VLLM_NIXL_SIDE_CHANNEL_PORT=30020 \
              exec vllm serve \
                "${ISVC_MODEL_PATH}" \
                --served-model-name "{{ "{{" }} deref .Spec "Model" "Name" {{ "}}" }}" \
                --port 8200 \
                --tensor-parallel-size {{ "{{" }} deref .Spec "Parallelism" "Tensor" | default 1 {{ "}}" }} \
                {{ "{{" }} if deref .Spec "Parallelism" "Expert" {{ "}}" }}--enable-expert-parallel{{ "{{" }} end {{ "}}" }} \
                ${_config_arg} \
                "${_kv_events_args[@]}" \
                $(ISVC_EXTRA_ARGS)
          env:
            - name: ISVC_PRESET_PATH
              value: ""
            - name: ISVC_PRE_PROCESS_SCRIPT
              value: ""
            - name: ISVC_USE_KV_EVENTS
              value: "false"
            - name: ISVC_MODEL_PATH
              value: '{{ "{{" }} deref .Spec "Model" "Name" {{ "}}" }}'
            - name: ISVC_EXTRA_ARGS
              value: >-
                --disable-uvicorn-access-log
                --no-enable-log-requests
            - name: UCX_TLS
              value: rocm_copy,rocm_ipc,self,sm,rc_x
            - name: VLLM_NIXL_SIDE_CHANNEL_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: PYTHONHASHSEED
              value: "12345"
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8200
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          volumeMounts:
            - name: shm
              mountPath: /dev/shm
      volumes:
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
