apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-mif-e2e
  namespace: {{.Namespace}}
spec:
  storageClassName: ceph-filesystem
  volumeName: model-mif-e2e
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Ti