apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models
  namespace: {{.Namespace}}
spec:
  storageClassName: ceph-filesystem
  volumeName: models-{{.Namespace}}
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Ti