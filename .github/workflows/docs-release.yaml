name: Docs release (Docusaurus → mif/publish-build)

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      PUBLISHED_BRANCH: publish-build
      DOCS_INCLUDE_CURRENT_VERSION: "false"

    defaults:
      run:
        working-directory: website

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: website/package.json

      - name: Install dependencies
        run: npm ci

      - name: Create docs version from tag
        run: npm run docs:version ${{ github.ref_name }}

      - name: Build Docusaurus site
        run: npm run build

      - name: Publish production docs to publish-build branch (mif repo)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          cd build
          git init
          git checkout -b "${PUBLISHED_BRANCH}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Publish docs for ${GITHUB_REF}"
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${REPO}.git"
          git push origin "${PUBLISHED_BRANCH}" --force
